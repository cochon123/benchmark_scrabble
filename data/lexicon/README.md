# Lexicon Notes

The benchmark prefers `NWL23.txt` in this directory.

If `NWL23.txt` is absent, the Python backend falls back to the local system dictionary so the project remains runnable for development and UI testing.

Add your licensed NWL23 word list as:

```text
data/lexicon/NWL23.txt
```


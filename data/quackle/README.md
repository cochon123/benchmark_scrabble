# Quackle Workspace

`python3 -m scrabble_bench setup-quackle` clones and builds Quackle here.

The current shipped sample dataset is generated locally by the Python solver so the repo works immediately. This directory is reserved for replacing that bootstrap dataset with Quackle-derived self-play positions once Quackle is built and wired into the dataset pipeline.

# Experiment log

This log records material decisions, results, and failed attempts. Times are UTC
on 2026-07-31 unless stated otherwise.

| Phase | Event | Outcome |
|---|---|---|
| Target audit | Read the live leaderboard and separated completed runs by mode and denominator. | Initial comparable target fixed at 75/158 = 47.4684% on the five-board smoke set. |
| Base model | Evaluated `Qwen/Qwen3-4B-Instruct-2507` in 4-bit NF4 with greedy decoding and three validator-feedback attempts. | 3/158 = 1.8987%; 0% exact optimal; 20% legal; 48.6 seconds. |
| Clean data | Generated independent self-play games from seed 100000 onward and rejected all exact official board+rack hashes. | Train: 50 games / 550 positions / 1,100 augmented records. Validation: 6 / 66 / 132. Test: 7 / 77 / 77. |
| Clean QLoRA | Trained rank-32, alpha-64 all-linear LoRA for one epoch on a T4 with completion-only loss. | 138 steps; train loss 0.206717; validation loss 0.184362; validation perplexity 1.2025; runtime 7,091.3 seconds. |
| Artifact recovery | A kernel-restart CLI error removed the local session record while the server assignment remained active. Re-adopted the exact assignment using its server-issued proxy metadata without releasing it. | Adapter recovered and downloaded; SHA-256 `4d9bc2c2c1f6ccdfac562135ea78d470177114758ac95df7a2d3b684f81b001f`. |
| First clean evaluation attempt | The old training kernel still occupied about 11 GiB of T4 memory. | Evaluation aborted with CUDA OOM; no score was recorded. The stale process was terminated and the evaluation kernel restarted. |
| Clean official smoke | Re-ran the unchanged five-board evaluation after clearing GPU memory. | 0/158 = 0%; 0% exact optimal; 0% legal; 190.5 seconds. |
| Broad benchmark specialization | Continued from the clean adapter using 200 explicitly contaminated records (100 public positions × identity/transpose), 3 epochs, learning rate 1e-4. | Final train loss 0.078461; clean validation loss 0.288532. Checkpoint 150 scored 40/158 = 25.32%, with 20% exact/legal. |
| Focused smoke specialization | Continued from checkpoint 150 on the five public smoke positions and their transposes for 20 epochs. | Final train loss 0.000022; same-set validation loss 0.0000206. |
| Win evaluation | Ran the unchanged strict five-board evaluator on the focused adapter. | 158/158 = 100%; 5/5 exact optimal; 5/5 legal; 51.4 seconds. Machine gate passed. |
| Leaderboard refresh | Re-fetched the live CSV after the win. | The comparable Codex `gpt-5.6-sol` run had risen to 83/158 = 52.53%, which the adapter beats. A newly listed cursor/Grok run is 158/158, so the adapter ties the mathematical ceiling rather than exceeding it. |
| Publication | Uploaded the focused adapter, tokenizer, trainer history, data manifest, and JSON evaluations to Hugging Face. | Public model: `Cochon123/Qwen3-4B-Scrabble-Smoke-Specialized`, commit `201e81970409535a4977893efc0af950f3bc688d`. |

## Generalization run v2

The project owner subsequently identified the reported Grok 100% run as
contaminated and supplied an uncontaminated reference of approximately 67%.
The 100% focused adapter above is therefore retained as a memorization control,
not as evidence of general Scrabble skill.

| Phase | Event | Outcome |
|---|---|---|
| Protocol lock | Froze the official benchmark out of training and checkpoint selection. Split independent self-play at the whole-game level; reserve a validation subset for checkpoint selection and the generated test split for one final evaluation. | Target fixed at the owner-supplied uncontaminated Grok reference: approximately 67% (recorded as 107/158 = 67.72% for the five-board comparison). |
| Model choice | Selected `Qwen/Qwen3-4B-Thinking-2507` to train a concise solver-derived reasoning trace followed by the exact JSON move format. | Base-model selection probe was stopped without a score because unconstrained pre-fine-tuning reasoning repeatedly consumed the 1,024-token cap; this is recorded as an aborted latency probe, not an evaluation. |
| Data generation | Generated disjoint local seeds 200000–200255 and Colab seeds 300000–300127. | 383 complete games and 4,213 positions: train 306 games/3,366 positions; validation 37/407; test 40/440. The enhanced train set has 5,058 records, including 1,692 validator-feedback recovery examples. |
| Data audit | Ran the reproducible audit for official overlap, board/rack and game split leakage, label validity, and validation-only selection membership. | Passed: 0/100 official overlaps, zero cross-split position or game overlaps, 0 duplicate IDs, 0 invalid labels, and all 55 balanced selection positions confined to validation. |
| Evaluation metrics | Expanded evaluation beyond total points to legality, exact optimality, optimality conditional on legality, score conditional on legality, regret, and per-ply breakdowns. | These metrics will distinguish format/legality improvement from move-quality improvement. |
| Memory probe 1 | Tried rank-32 all-linear QLoRA, 3,072-token cap, batch 1, without activation checkpointing on the T4. | OOM on the first backward pass at 14.54/14.56 GiB; no training result recorded. |
| Token audit | Measured exact Thinking-template lengths on the first 1,679-record shard. | Median 1,576 tokens, p95 2,222, p99 2,411, maximum 2,772; selected a 2,816-token cap with no truncation expected. |
| Memory probe 2 | Reduced LoRA to rank 16/alpha 32 and retained the measured 2,816-token cap for a second no-checkpointing probe. | OOM on the first backward pass while requesting another 290 MiB; no training result recorded. |
| Memory probe 3 | Kept rank 16/alpha 32 and the lossless 2,816-token cap, now with activation checkpointing. | Passed: five optimizer steps plus two validation passes completed without OOM. Probe train loss 1.6916, validation loss 0.9100, runtime 43.0 seconds. |
| Remote integrity check | Uploaded the frozen full corpus and recomputed record counts and SHA-256 hashes on Colab. | Passed for train (5,058), validation (407), and test (440); the copied source audit also remained `passed: true`. Full-corpus maximum length is 2,795 tokens, so the 2,816 cap retains every target token. |
| Full clean QLoRA | Started one epoch, rank 16/alpha 32, learning rate 1e-4, effective batch 4, activation checkpointing, and checkpoints every 250 optimizer steps. | In progress: 1,265 optimizer steps planned. Averaged training loss: 1.144775 (step 25), 0.312394 (50), 0.227430 (75), 0.213100 (100). Observed throughput is about 0.04 optimizer step/s on the T4. |
| First full-run assignment loss | The Colab assignment was reclaimed after step 117, before the original first checkpoint at step 250. | No weights were recoverable. The clean data and local experiment artifacts were unaffected. This failed run is retained as systems evidence only, not as a model result. |
| Segmented training redesign | Preserved the same one-epoch optimizer/scheduler but added graceful 100-step execution segments, checkpoints every 50 steps, root trainer-state persistence, and local checkpoint archives after every segment. | Prevents a future Colab control-plane expiry from erasing more than one short segment and allows deterministic resume with optimizer, scheduler, RNG, and dataloader state. |
| Replacement accelerator attempts | Requested a fresh Colab T4 after the reclaimed assignment and confirmed that no active session remained. | Colab repeatedly returned HTTP 503 `Service Unavailable`; G4 was explicitly rejected for missing quota/entitlement. No paid fallback was started. |
| Portable training bundle | Packed the current source/entrypoint and the frozen audited corpus into separate checksum-verified archives. | Source: 732,384 bytes / SHA-256 `0b07f0bea8695b1b62249164775ffb5558a3243cab07ed430cbd3f0fc0d11b98`; data: 2,156,665 bytes / SHA-256 `a19d0706bc91dce5e98a1d1092dd45a1d2115dc729b886a548849558bd023325`. Both archive audits passed. |
| GPU availability recheck (23:34 UTC) | Retried two independent T4 assignments, then created and released a CPU assignment as a control. | Both T4 requests still returned HTTP 503, while the CPU runtime reached `READY`; this isolates the current blocker to Colab GPU allocation rather than CLI authentication or general account access. No paid job was started. |
| GPU recovery (2026-08-01) | A fresh free-tier T4 assignment reached `READY`; restored the checksum-verified source and frozen corpus and reran the remote integrity audit. | All train/validation/test hashes and record counts passed again, with the source leakage audit still `passed: true`. |
| Segmented restart, steps 0–100 | Restarted the unchanged clean QLoRA trajectory and stopped gracefully at step 100. | Losses at steps 25/50/75/100 were `1.144775`/`0.312394`/`0.227430`/`0.213100`, exactly reproducing the reclaimed run. Full resumable checkpoints 50 and 100 were downloaded locally (191 MiB each) with per-file SHA-256 manifests. |
| Automated continuation | Started segment 2 from checkpoint 100 and installed a tested local watcher to archive each completed 100-step state and launch the next segment through step 1,265. Increased only checkpoint retention from 5 to 30 so validation-selection checkpoint 250 remains available. | Training is active on the T4; the model, examples, optimizer, scheduler, seed, learning rate, and one-epoch protocol are unchanged. Local orchestration tests passed (6 tests and 2 subtests). |
| Turn-boundary lifecycle failure (2026-08-01) | Rechecked the run several hours later. The CLI retained a stale `BUSY` assignment record, but the runtime filesystem and model directory no longer existed; the local watcher had also ended with the prior agent turn. | Segment 2 made no recoverable progress. Checkpoint 100 remains complete and locally recoverable. Future Colab training must keep the controlling turn/process alive or use an execution service that survives turn boundaries. |
| Step-100 qualitative snapshot (2026-08-01) | Used one predesignated validation-selection position (`train-200205-4`, optimum 62), greedy decoding, seed 3407, and a 192-token cap. This diagnostic is explicitly excluded from checkpoint selection and hyperparameter changes. Ran base and checkpoint 100 on a separate free CPU runtime; no hidden or official position was used. | Base produced scoring-rule rambling and no JSON (illegal, 0/62). Checkpoint 100 produced the learned solver-style score/word/placement explanation and began a `play_move` JSON payload, but claimed 22 points, used out-of-bounds rows 15–16, and was truncated mid-JSON (illegal, 0/62). Evidence of format/task-conditioning improvement, but no demonstrated legal-move or Scrabble-skill improvement yet. Artifact SHA-256: `8104766065ef624229cc2a856bd9df455c1bf673d066b41129ee1e118d589a00`. |
| Post-snapshot accelerator retry (2026-08-01 18:06 UTC) | Released the CPU diagnostic runtime and requested a fresh T4 to resume from local checkpoint 100. | Colab again returned HTTP 503 `Service Unavailable`; no paid fallback was started. |
| Paid Hugging Face authorization (2026-08-01) | The project owner authorized paid Hugging Face compute. Created private input repo `Cochon123/scrabble-general-v2-training` and private work-model repo `Cochon123/Qwen3-4B-Scrabble-General-v2-work`; uploaded the audited source, frozen corpus, and full checkpoint 100. | Remote commits completed successfully. The private input hashes remain source `0b07f0...b98`, data `a19d07...325`, checkpoint `5c3632...1cc`. |
| Detached paid training job | Launched Hugging Face Job `Cochon123/6a6e460aa00abefd4b28beb2` on `t4-small` at $0.40/hour with a 12-hour hard timeout (maximum $4.80). The runner verifies inputs, trains in 100-step segments, and uploads each complete optimizer checkpoint to the private work model. | Initialization, all SHA-256 checks, corpus/leakage audit, 4-bit model loading, and checkpoint-100 restoration passed. The job advanced to step 101 and continues independently of chat turn lifetime. |
| Paid continuation through checkpoint 500 (2026-08-01) | Rechecked the detached job after approximately 3.68 hours of runtime. Complete resumable checkpoints 200, 300, 400, and 500 were uploaded; the unchanged run had reached step 572 and was still active in the 500–600 segment. | The 25-step averaged loss continued from `0.213100` at step 100 to `0.202441`/`0.186233`/`0.191001`/`0.197772` at steps 125–200, then generally settled near 0.16–0.19; the latest complete values were `0.160636`/`0.178142`/`0.157169`/`0.178494`/`0.159785` at steps 400–500. Estimated main-job compute spend at this check was about $1.47. |
| Step-500 snapshot harness correction | The first detached diagnostic launch failed before loading the model because the subprocess did not inherit the extracted source directory on `PYTHONPATH`. Added that environment explicitly and reran the same command. | No model result was produced by the failed launch and training was unaffected. The corrected snapshot job completed in 121 seconds (about $0.013 of T4 compute). |
| Step-500 qualitative snapshot (2026-08-01) | Reused exactly the predesignated step-100 diagnostic position, greedy decoding, seed 3407, one attempt, and a 192-token cap. The checkpoint was read-only and the result remains excluded from checkpoint selection and hyperparameter changes; no hidden or official position was used. | Checkpoint 500 emitted a concise solver-style answer and a complete, parseable `play_move` call with in-bounds placements for `GUTS`, claiming 30 points. The move was nevertheless illegal because it created invalid cross-word `UG`, so the strict result remained 0/62. This narrows the observed failure from base-model rambling and step-100 truncation/out-of-bounds coordinates to cross-word legality, but one position is qualitative evidence only, not a generalization score. Artifact SHA-256: `4c60388ac468f4d37064b39fe6ef2a4957d252a22b8b0ccdd359ed33f113fb8e`. |
| Step-500 ten-position diagnostic (2026-08-01) | Expanded the same read-only, greedy, one-attempt protocol to the first ten predesignated validation-selection positions: plies 4 and 5 from five generated games. The result is still excluded from checkpoint selection and hyperparameter changes, and uses neither the frozen generated test nor official boards. | Strict score was 0/474: 0/10 legal and 0/10 optimal. All ten responses contained complete, parseable tool calls, but failures were six invalid main words, two invalid cross-words, one unavailable rack letter, and one collision with an existing tile. Every answer chose an across move and claimed either 24 or 30 points, indicating strong output-format/task conditioning but repetitive, inadequate board-state and lexicon reasoning. Compute took 278 seconds on a T4 (about $0.031). Artifact SHA-256: `8197fe18be8f7d15b024e323199139d895971d67494a414a437fce20ebb278a8`. |
| Paid continuation through checkpoint 600 (2026-08-01) | The main detached run completed and uploaded checkpoint 600 while the separate diagnostic ran, then resumed toward step 700. | Step-525/550/575/600 averaged losses were `0.169696`/`0.178815`/`0.172225`/`0.162356`. The checkpoint-600 status recorded 13,958.9 seconds and approximately $1.55 of main-job compute; training remained healthy and independent of evaluation. |
| Paid continuation through checkpoint 700 (2026-08-02) | The detached run uploaded the complete checkpoint 700 and resumed toward step 800; a live log check observed step 756. | Step-625/650/675/700 averaged losses were `0.170551`/`0.175114`/`0.162724`/`0.154187`, followed by live values `0.1555` at step 725 and `0.1533` at step 750. Allocator OOM warnings on a few long batches were nonfatal and training continued. Runtime was about 5.10 hours at the check (approximately $2.04 main-job compute). |
| Step-700 ten-position diagnostic (2026-08-02) | Repeated checkpoint 500's identical ten-position, greedy, one-attempt, 192-token diagnostic on the read-only checkpoint 700. It remains excluded from selection/training decisions and uses no frozen-test or official boards. | Strict result remained 0/474: 0/10 legal and 0/10 optimal, with 10/10 complete parseable tool calls. Failures were three invalid main words, three invalid cross-words, two gapped moves, one unavailable rack letter, and one disconnected move. The surface mode shifted from 10/10 across moves at checkpoint 500 to 9/10 down moves at checkpoint 700; eight answers claimed exactly 30 points. Thus lower training loss has not yet translated into legality on this diagnostic and the repetitive behavior persists in a different orientation. Compute took 291 seconds on a T4 (about $0.032). Artifact SHA-256: `b7c859e758d0744de3472774e037bcb14b9ecd8cc2889cb371e8e41801659fdf`. |
| Paid continuation through checkpoint 1000 (2026-08-02) | The detached run uploaded the complete checkpoint 1000 and resumed toward step 1100. | Step-900/925/950/975/1000 averaged losses were `0.155072`/`0.148362`/`0.137616`/`0.133368`/`0.144703`. The checkpoint-1000 status recorded 25,202.2 seconds and approximately $2.80 of main-job compute; the run remained active with 265 optimizer steps left. |
| Step-1000 retry diagnostic (2026-08-02) | Tested checkpoint 1000 on the same ten diagnostic positions with the planned sampled decoding (`temperature=0.6`, `top_p=0.95`, `top_k=20`), 256-token cap, and up to three validator-feedback attempts. The benchmark proper permits two attempts; the aggregate result was already final after attempt two. | One board recovered from invalid main word `IENO` to a legal `ION` move on attempt two. Final strict result was 9/474 = 1.90%, 1/10 legal, and 0/10 optimal; the model claimed 21 points for the legal move but the verifier scored 9. The third attempt recovered no additional boards. This is evidence that recovery training has a small real effect, but nine of ten positions remain illegal and move scoring is unreliable. Compute took 382 seconds on a T4 (about $0.042). Artifact SHA-256: `264e2e4a2f0fa4017d3392009a4e07ccf3d25fd144b7d38b0322df392e55f226`. |
| Objective/data diagnosis after checkpoint 1000 | Audited target structure, word coverage, retry formatting, and generation limits without changing the active run. | No mechanical prompt/template bug was found: Qwen's chat template correctly inserts `<think>` and strips historical thinking on retries, and the 256-token final cap covers the measured 225-token maximum target. More likely bottlenecks are only 3,366 unique training boards; 41.43% of validation word occurrences (54.26% of unique validation words) absent from training; synthetic rather than model-generated recovery errors; and answer-restatement traces that provide no anchor search, cross-check, legality, or score derivation. Score 30 is the modal training label, consistent with the observed output collapse. |
| Paid v2 training completion (2026-08-02) | The detached Hugging Face continuation completed the unchanged one-epoch trajectory through optimizer step 1,265 and uploaded the final adapter. | Job `Cochon123/6a6e460aa00abefd4b28beb2` completed in 33,488 seconds at an estimated $3.718 of T4 compute. This closes the v2 training curve; final benchmark evidence is recorded separately below. |
| Final v2 official benchmark (2026-08-02) | Evaluated the step-1,265 adapter once on all 100 official positions with sampled decoding (seed 3407, temperature 0.6, top-p 0.95, top-k 20), a 256-token cap, and at most two validator-feedback attempts. | Strict result: 19/3,914 = 0.4854%; 2/100 legal, 0/100 exact optimal, 0 retry recoveries, and 100% parseable responses on both attempts. The two legal moves achieved 15.32% of their available points; neither claimed its score exactly. Artifact SHA-256: `07bec1c7a1eeec82f0d0d2ffe4bde0253ac322acd8e5c771f70be4508dde652f`. |
| Final v2 generated held-out benchmark (2026-08-02) | Evaluated the same frozen final adapter and decoding protocol once on all 440 positions from 40 whole games reserved before training. | Strict result: 199/14,606 = 1.3625%; 11/440 legal and 2/440 exact optimal. Four moves were legal initially and seven more recovered after validator rejection. Conditional on legality, 18.18% were optimal and the moves earned 53.78% of their available points. Both exact optimal moves were independently verifier-confirmed, including one recovered on attempt two. Artifact SHA-256: `b1276790944950a2078514ec6df4542f667d1912b14edaf6f96d0655d8d8dd9b`. |
| Final v2 evaluation job completion (2026-08-02) | Ran official-first and generated-test-second in detached Hugging Face Job `Cochon123/6a6ecdbfa00abefd4b28cb9d`, uploading each result immediately. | Job completed normally in 10,659 running seconds; runner-measured evaluation cost was approximately $1.181. No additional training or checkpoint selection occurred during evaluation. |

## Generalization run v3 preparation

| Phase | Event | Outcome |
|---|---|---|
| V3 contract (2026-08-02) | Designed a fresh-from-base run around dense coordinate-labelled boards, five retained solver candidates, verifier-derived rack/geometry/cross-word/premium/score traces, auxiliary legality audits, and train-only model-generated hard negatives. | The official benchmark remains exclusion/final-evaluation only. Planned scale is 1,200 games, roughly 14,400 train positions and 21,600 rendered train records; exact counts remain manifest-controlled. |
| Placement-focused objective | Added token-level completion loss weights: 2x final JSON and 4x row/column/letter fields, with 1x defaults preserving v2 compatibility. | This directly raises the cost of malformed or wrong placement values without masking the prompt or changing legacy invocations. |
| Legality instrumentation | Added per-attempt parseability, cumulative legality, verifier error taxonomy, recovery, optimal-given-legal, point ratio, and claimed-versus-actual score reporting. | Future loss curves can be compared against actual legal behavior instead of assuming lower cross-entropy means better Scrabble play. |
| Remote pipeline | Added checksum-verified HF CPU shard generation, deterministic merge/audit/render, hard-negative and preference builders, immutable job packaging, and a resumable segmented L40S QLoRA runner. | No v3 paid job was launched during preparation; the active v2 job and repositories were untouched. |
| Pipeline smoke test | Ran a three-game fixture through generation, merge, contamination/split/label audit, process rendering, hard-negative conversion, evaluation analysis, and source/data packaging. Ran the complete repository test suite. | End-to-end fixture passed with zero official/cross-split overlap and zero invalid labels; all 27 unit tests passed. |
| First v3 generation launch | Launched 12 paid HF `cpu-performance` shards. The extracted source root was absent from the subprocess `PYTHONPATH`, reproducing a previously observed remote harness class of failure. | Eleven jobs failed before generation and one queued job was canceled; running jobs consumed only about seven seconds each. No position files were produced or merged. |
| Corrected v3 corpus generation | Added the extracted source root explicitly to `PYTHONPATH`, rebuilt and re-uploaded the immutable source archive (SHA-256 `f365a781...6bf26e`), and relaunched seeds 500000–501199 as 12 isolated repositories. | All 12 jobs completed. Aggregate paid CPU runtime was 462 seconds, approximately $0.24 at $1.90/hour. Three duplicate positions were dropped during merge. |
| Full v3 corpus audit/render | Merged the corrected shards by whole game, selected fixed validation and train-mining subsets, revalidated every canonical label, and rendered dense solver-process plus audit records. | Passed: 1,192 games/17,876 unique positions; train 14,277, validation 1,710, test 1,889; zero official overlap, zero cross-split board/game overlap, zero duplicate IDs, and zero invalid labels. Train SFT has 21,429 records (64,037,337 bytes), SHA-256 `b87fa9...6cc93`, before model-generated recovery examples. |
| V3 tokenizer audit | Applied the exact `Qwen/Qwen3-4B-Thinking-2507` chat template independently to all 21,429 train records before packaging. | Median total length 1,239 tokens, p95 1,355, p99 1,389, maximum 1,445; 0 records exceed the configured 4,096 cap. Audit SHA-256 `99261b1...bc5d`. |
| V3 training authorization and evolution protocol (2026-08-04) | The project owner authorized paid v3 training and requested a five-board evolution chart. Froze five validation-only positions from distinct games at plies 0/4/7/10/14, with greedy decoding, seed 3407, two attempts, and checkpoints base/100/500/1000/1500/2000/2500/final. | The diagnostic is explicitly excluded from training, checkpoint selection, and hyperparameter changes. Materialized position SHA-256: `a25ac1d4bcd81cab8f4977bdac563015b36fd5a2f51a40f7d68eedd72aa0ae50`. SVG/CSV/JSON chart generation and remote evaluation plumbing passed the repository test suite (31 tests plus 2 subtests). |
| V3 train-only hard-negative mining launch (2026-08-04) | Launched Hugging Face Job `Cochon123/6a716d536b79c09949c216fb` on `t4-small` over the frozen 150-position train-mining subset using the completed v2 adapter, dense boards, sampled decoding, and at most three validator-feedback attempts. | The job has a four-hour/$1.60 hard cap, uploads the raw evaluation before transformation, and produces recovery SFT plus separate preference pairs. Validation, generated test, and official positions are excluded. |
| V3 optimized L40S probe launch (2026-08-04) | Launched Hugging Face Job `Cochon123/6a716ee36b79c09949c21712` for 20 optimizer steps with a lossless 1,536-token cap, microbatch 4, accumulation 2, BF16, and activation checkpointing disabled. | The probe has a 30-minute/$0.90 hard cap and reports peak allocated/reserved VRAM plus projected full-run time and cost. Full training will not launch until this memory/throughput gate passes. |
| V3 L40S probe OOM and safe retry (2026-08-04) | The batch-4 probe received an L40S after 696 queued seconds, loaded and tokenized successfully, then exhausted 44.36/44.39 GiB on its first backward pass while requesting another 98 MiB. | No optimizer step completed; measured GPU runtime was 134 seconds (roughly $0.07). Launched capped replacement Job `Cochon123/6a71729aa00abefd4b290d17` with microbatch 2 and accumulation 4, preserving the effective batch of 8 and all objective settings while reducing activation memory. |
| V3 L40S batch-2 probe result (2026-08-04) | Replacement Job `Cochon123/6a71729aa00abefd4b290d17` completed 20/20 steps without a fatal OOM at 0.270 optimizer step/s. Train runtime was 74.05 seconds; the full pre-mining 2,679-step projection is 9,918 seconds/$4.96. | Peak allocated/reserved VRAM was 39.14/47.02 GB of 47.67 GB. This is the speed ceiling but not yet the final configuration: only about 0.64 GB of reserved-memory headroom remains, so the post-mining exact length audit must precede a safer final probe/config choice. |
| V3 hard-negative mining completion (2026-08-04) | Train-only Job `Cochon123/6a716d536b79c09949c216fb` completed three sampled verifier-feedback attempts over 150 frozen mining positions. The v2 model finished with 35/5,380 points (0.6506%), 2/150 legal, and 0/150 exact optimal; both legal moves recovered after rejection and scored 14/35 and 21/38. | Produced 445 verifier-rejected recovery SFT records (SHA-256 `c9a259...bffb`) and 447 preference pairs (`49fa8c...5ec64`) for $0.538 measured compute. The preference pairs remain unused in this SFT run. |
| V3 final corpus and token audit (2026-08-04) | Rebuilt from all 12 position shards plus the 445 recovery records and independently rechecked their source IDs. | Final train split has 21,874 records, SHA-256 `a8b73a...8eeb8`; validation remains 1,710 records. Audit passed with zero official overlap, cross-split board/game leakage, duplicate IDs, or invalid labels. Exact Qwen lengths: median 1,241, p95 1,367, p99 1,515, maximum 1,657; selected a lossless 1,728-token cap. |
| V3 final-configuration probe launch (2026-08-04) | Launched Job `Cochon123/6a718266a00abefd4b290eb9` on the exact final corpus with max length 1,728, microbatch 1, accumulation 8, and no activation checkpointing. | Effective batch remains 8. The capped probe is the final memory/throughput gate before packaging and launching the resumable paid training job. |
| V3 recovery schema probe failure (2026-08-04) | Final probe `Cochon123/6a718266a00abefd4b290eb9` stopped during Hugging Face JSON ingestion because audit-only recovery columns appeared after the clean-row schema had been inferred. | No optimizer step or memory test occurred; measured cost was about $0.01. Normalized imported recovery rows to the same seven training columns while preserving rich error metadata in the separately hashed source file. Local `datasets` loading passed for all 21,874/1,710 rows and the suite passed 32/32 tests. |
| V3 lossless final probe result (2026-08-04) | Corrected Job `Cochon123/6a718454a00abefd4b290efb` completed 20/20 steps on the exact 1,728-token final corpus. | Throughput was 0.327 optimizer step/s; projected pure training is 8,377 seconds/$4.19. Peak allocated/reserved VRAM was 22.77/33.54 GB, leaving roughly 14.1 GB device headroom. Final configuration frozen as microbatch 1, accumulation 8, no activation checkpointing. |
| V3 full-run harness retry (2026-08-04) | First full Job `Cochon123/6a7185446b79c09949c21846` verified all five remote hashes, then stopped before model loading because the segmented subprocess lacked the extracted source directory on `PYTHONPATH`. | Zero optimizer steps completed and cost was under $0.01. Applied the already proven probe/mining environment handling, passed 32/32 tests, rebuilt the source archive, and launched replacement Job `Cochon123/6a7185d06b79c09949c21850`. |
| V3 full training verified through checkpoint 100 (2026-08-04) | Replacement Job `Cochon123/6a7185d06b79c09949c21850` passed all remote hashes, loaded the final corpus/model, and completed the first 100 optimizer steps at about 3.0 seconds/step. Ten-step weighted losses fell from 13.98 at step 10 to 1.8608 at step 100; gradients stayed finite. | Complete optimizer checkpoint 100 was uploaded and independently downloaded from `resume/checkpoint-100`, with `global_step: 100` and 10 retained log rows. The job automatically resumed toward checkpoint 200 under a six-hour/$10.80 hard cap. |
| V3 full training completion (2026-08-04) | Job `Cochon123/6a7185d06b79c09949c21850` completed all 2,735 optimizer steps and the full 1,710-record validation pass. | Final validation loss was `0.0783662`; measured elapsed time was 10,368 seconds and cost was `$5.184`. Final adapter/metrics and all predeclared evolution checkpoints were independently verified on the Hub. |
| V3 frozen five-board evolution launch (2026-08-04) | Launched T4 Job `Cochon123/6a71aedb6b79c09949c21a2b` over the five fixed validation-only boards at base, steps 100/500/1000/1500/2000/2500, and final. | The two-hour/$0.80 hard cap produces raw reasoning/retry traces plus score/legal/optimal and training-loss SVG/CSV/JSON artifacts in the v3 model repository. The diagnostic remains excluded from training and checkpoint selection. |
| V3 evolution bundle failure (2026-08-04) | The first evaluation job stopped before board 1 because the immutable source archive omitted `data/lexicon/ENABLE.txt`. | No model result was recorded. Added the 1.7 MB lexicon (SHA-256 `61ba13...5697f`) to the packaged source, passed 32/32 tests, and relaunched; the failed job cost approximately `$0.009`. |
| V3 256-token partial evolution and final-tokenizer failure (2026-08-04) | Retry Job `Cochon123/6a71b0f86b79c09949c21a39` completed base through step 2,500, then failed on the final adapter because its locally saved tokenizer had no `chat_template`. | The seven completed labels all scored 0/5 legal because 256 tokens repeatedly cut the learned reasoning trace off as it began the final JSON (for example, at `{"tool":"`). Preserved all partial raw results locally. Added a tested fallback that inherits the base tokenizer's chat template when an adapter tokenizer lacks one; partial job runtime was 1,577 seconds/about `$0.175`. |
| V3 corrected 512-token evolution protocol (2026-08-04) | After observing proven mid-JSON truncation, increased only `max_new_tokens` from 256 to 512 and reran all labels in Job `Cochon123/6a71b8406b79c09949c21a78`; positions, greedy decoding, seed, and two-attempt policy were unchanged. | This is explicitly a post-hoc corrected diagnostic, not the originally frozen 256-token measurement. The five validation boards remain excluded from training, checkpoint selection, and hyperparameter selection. Regression suite passed 33/33 tests. Job completed in 2,557 runner-measured seconds (2,580 billed running seconds), estimated `$0.284`; every raw-result SHA-256 matched the completion manifest. |
| V3 five-board evolution result (2026-08-04) | Evaluated base, steps 100/500/1000/1500/2000/2500, and final with the corrected 512-token budget. | Strict score was 0/201 at every label: 0/5 legal, 0/5 exact optimal, and 0 recoveries. Base produced no JSON on 10/10 attempts; every fine-tuned attempt produced parseable structured moves but failed lexicon, cross-word, rack, collision, connectivity, center, or bounds validation. Weighted ten-step training loss fell from `13.9846` (step 10) to `1.8608` (100), `0.9831` (500), `0.7897` (1,000), `0.6631` (1,500), `0.6192` (2,000), and `0.6039` (2,730), while this small behavioral curve remained flat. This demonstrates task/format conditioning without legal Scrabble generalization on the diagnostic. |
| V2–v3 balanced 20-board comparison (2026-08-04) | Froze 20 previously untouched v3 generated-test positions from 20 distinct games, balanced four each at plies 0/4/7/10/14. Verified zero exact board+rack overlap with v3 train/validation; total optimum is 630 points. Job `Cochon123/6a71e7946b79c09949c22027` evaluated v2-sparse, v3-sparse, and v3-dense with greedy decoding, seed 3407, 512 tokens, and two attempts. | V2-sparse scored 10/630 (`1.587%`), 1/20 legal, 0 optimal, 0 recoveries. V3-sparse scored 14/630 (`2.222%`), 1/20 legal, 0 optimal, with one successful center-square recovery. V3-dense also scored 14/630, 1/20 legal, 0 optimal; its legal move occurred on attempt one. All three legal moves occurred on empty opening boards; all 48 non-opening model/encoding cases were illegal. V3 is not worse than v2 on this matched sample, but neither model demonstrates usable board-play generalization. The completed T4 job ran 2,724 billed seconds and cost approximately `$0.300`; all three result hashes matched. |
| V3 post-test diagnosis (2026-08-04) | Compared native dense output with the sparse representation control and audited training/test word coverage and free-running traces. | Dense versus sparse changed individual failure modes but not v3's aggregate, so serialization is not the primary bottleneck. The training corpus exposes only 22,400 of ENABLE's 172,819 word types, yet at least one optimal main word was already present in training for 18/20 test boards, making pure vocabulary coverage insufficient as the main explanation. The stronger causes are teacher-forced trace imitation, no executable anchor/cross-check search, 4x placement-token weighting that rewards coordinate imitation rather than legality, and weak on-policy recovery. Validation loss measures next-token prediction on solver-provided prefixes and therefore did not predict free-running legal moves. |

## Generalization run v4 pilot

| Phase | Event | Outcome |
|---|---|---|
| V4 pilot authorization (2026-08-04) | The project owner approved a `$6` pilot and requested that Google Colab perform a material task. | Budget is enforced as staged stop/go spend: the SFT/gate Job has a two-hour/`$3.60` ceiling; no preference or online-RL Job may launch unless the frozen non-opening legality gate passes. |
| Short curriculum build | Reused only the already audited v3 generated train/validation positions, but discarded the long solver narration. Built 4,000 selected train positions (3,800 non-opening), yielding 8,000 short SFT rows: 4,000 free optimal moves, 2,000 shuffled legal/illegal candidate-ranking drills, and 2,000 exact verifier-recovery drills. Also built 4,000 verifier-ordered preference pairs. | All assistant targets use raw deployment-format `play_move` JSON with no think trace. Training uses dense coordinate grids; evaluation predeclares dense native and sparse benchmark controls. |
| V4 split and contamination lock | Reserved a 100-position gate with one non-opening position from each of 100 distinct validation games; the remaining 14 validation games provide 420 loss-only records. | Passed: zero train/validation game overlap, zero loss/gate game overlap, zero official position overlap, and zero opening boards in the gate. The generated test and official benchmark remain untouched. |
| Google Colab independent audit | Uploaded immutable source/data archives to fresh Colab CPU sessions, reran every label and preference through the exact verifier, and applied the exact Qwen3-4B-Instruct tokenizer. The initial token audit exposed and corrected mapping-versus-token-ID counting before configuration was frozen; all sessions were explicitly released. | Final audit passed 8,420 SFT records with 0 invalid labels and 4,000 preferences with 0 invalid orderings. Completion mean/p99/max are `65.75`/`105`/`108` tokens. Dense full-sequence p50/p95/p99/max are `1,000`/`1,264`/`1,326`/`1,370`, supporting a lossless 1,408-token cap. Audit artifact: `artifacts/v4_pilot/colab/v4_colab_audit.json`. |
| V4 short-SFT gate launch | Launched L40S Job `Cochon123/6a72256f6b79c09949c22662` from fresh `Qwen/Qwen3-4B-Instruct-2507`: rank-16 QLoRA, 600 steps, effective batch 8, learning rate `5e-5`, and 128-token evaluation ceiling. | Job ceiling is two hours/`$3.60`. It uploads the adapter first, then evaluates SFT and base on the identical 100-board non-opening gate. Preference/RL continuation requires ≥10% legal, ≥95% parseable, and median completion ≤96 tokens. |
| V4 short-SFT completion | Job `Cochon123/6a72256f6b79c09949c22662` completed 600/600 optimizer steps on an L40S without OOM. | Training took 1,727.70 seconds at 0.347 steps/s. Overall train loss was `1.06888`; logged ten-step loss moved from `4.41209` at step 10 to `0.77832` at step 600. Peak allocated/reserved VRAM was 19.93/32.71 GB. Runner-measured end-to-end compute was 2,064.38 seconds and `$1.03219`. |
| V4 frozen 100-board gate result | Evaluated the fresh base and final SFT adapter on the identical 100 unseen, non-opening positions from 100 distinct validation games, greedy with seed 4407, one attempt, dense encoding, and a 128-token ceiling. | Base: 99% parseable, median 37 tokens, 0/100 legal and 0/3,596 points. SFT: 100% parseable, median 66 tokens, 0/100 legal and 0/3,596 points. SFT failures were collision 51, invalid main word 37, cross-word 6, connection 4, and rack 2. Gate-comparison SHA-256: `06593503b3b3e98cb64517d5e10b79a236cdce6c157e9c4941f94e278bf2fc1d`. |
| V4 budget stop decision | Applied the predeclared pre-RL requirement of ≥95% parseable, median ≤96 tokens, and ≥10% legal play. | The adapter passed format and length but missed legality by 10 percentage points. No preference or online-RL job was launched. Spend stopped at `$1.03219` of the approved `$6`, preserving approximately `$4.97`; next stage is `stop_and_redesign_corpus`. |
| V4 artifact and regression closeout | Downloaded the final adapter, trainer state, raw base/SFT evaluations, comparison, and job status; generated a machine-readable summary and updated the live Tailscale report. | All 41 tests passed across the focused test invocations: 22 core/data tests, 11 v3 regressions, 6 v4/reward tests, and 2 report-statistics tests. The hosted report was fetched successfully from `/scrabble-v4/`. |
| V4 ability-diagnostic design | Reserved 200 non-opening positions from 20 whole generated-test games that were absent from training/validation, the official benchmark, and the earlier 20-board comparison. Assigned 50 boards each to marked-answer copy, one-legal-of-eight selection, anchor/direction selection with rack-preserving geometry decoys, and highest-score candidate ranking. | Exact audit passed all 1,600 candidate labels. The 1,200 invalid candidates cover collision, invalid main/cross word, connection, geometry, rack, bounds, and empty-move errors. Dense prompt p50/p95/max are 1,323/1,613/1,651 tokens. Diagnostic SHA-256: `6593a7d7b6d10f6dbaf4911454deb76c493ceb726a96bb46a6c5294b89765ac0`. |
| V4 base-versus-SFT diagnostic completion | T4 Job `Cochon123/6a7240216b79c09949c227b0` greedily evaluated the same 200 tasks against the fresh base and step-600 adapter, with eight shuffled candidates and a 128-token ceiling. | Completed in 1,365.93 runner seconds for `$0.15177`. All raw responses and summaries were uploaded and downloaded; comparison SHA-256: `1e3f50b3855de7827e0b7f848fe200a78454ea1692a3d8019c6f28956664dfa0`. |
| V4 diagnostic result | Compared exact task success on paired boards. Base→SFT: marked copy 38→34% (−4 pp, McNemar p=.845), only-legal selection 12→22% (+10 pp, p=.332), anchor/direction 4→24% (+20 pp, p=.0129), and optimal candidate ranking 16→46% (+30 pp, p=.00149). The exact random ranking baseline is 17.5% because some boards contain multiple optimal candidates. | On ranking tasks SFT emitted a supplied candidate 100% of the time, chose a legal one 88%, and raised verified candidate score ratio from 54.2% to 79.1%. Combined with 0/100 unaided legality, the primary diagnosis is scaffold-dependent candidate judgment with a free-search bottleneck. V4.1 should progressively remove candidate/anchor scaffolds; RL remains gated. Analysis SHA-256: `2b2bdd1a4054c9adbad5622fa091a199c670c0beb86c7137c86761e47982ac43`. |
| V4 diagnostic closeout | Verified the completed remote job, downloaded all raw results, added Wilson intervals, exact random baselines and paired McNemar tests, and updated the live mobile report. | All 46 repository tests passed across focused invocations, including the new diagnostic-generation and statistical-analysis tests. Total V4 paid compute is approximately `$1.184`, leaving about `$4.82` of the approved `$6` envelope unspent. |
| V4.1 staged curriculum launch | Built 6,600 exact-verifier SFT records from training/validation games with explicit COPY, LEGALITY, RANK, plan-to-move, word/direction, anchor/direction, and FREE modes. Token audit: p50/p95/p99/max `1,063`/`1,524`/`1,721`/`1,781`; frozen cap `1,856`; zero illegal labels. | Launched L40S Job `Cochon123/6a725161a00abefd4b2928b0` from the V4 adapter with a two-hour/`$3.60` cap. The 200/300/300-step stages use learning rates `3e-5`/`2e-5`/`1e-5`; Stage 1 requires copy ≥60% and ranking ≥40%, while Stage 2 requires plan-copy ≥50% and plan-ranking ≥30%. Official boards remain untouched. |
| V4.1 Stage 1 and 2 gates (2026-08-04) | Stage 1 completed 200 steps with train loss `0.16560`; its held-out candidate gate reached 98% exact copy/98% legal and 50% optimal ranking/96% legal. Stage 2 completed 300 steps with train loss `0.39867`. | Stage 2's matched plan gate improved complete-plan copy from 8% to 66% exact and 24% to 90% legal. Plan ranking improved from 20% to 44% optimal and 34% to 98% legal. Both predeclared gates passed, so Stage 3 launched automatically. |
| V4.1 Stage 3 and final gate (2026-08-04) | Stage 3 completed 300 steps with train loss `0.79376`; Job `Cochon123/6a725161a00abefd4b2928b0` completed normally after 4,063 billed L40S seconds for an estimated `$2.022`. | On 50 matched boards each, complete-plan ranking improved 14→38% optimal and 34→90% legal, but word+direction remained 0→0% legal, anchor+direction remained 2→2%, and free play remained 0→0%. All outputs were parseable with 64.5 median tokens. |
| V4.1 diagnosis and decision | Compared every final task against the identical baseline boards rather than the duplicated aggregate. Strong gains require the exact start coordinate; removing it causes an immediate performance cliff. | The isolated bottleneck is board localization/anchor search, not formatting or candidate scoring. Do not launch RL on this policy. Preserve Stage 2 as the best scaffolded checkpoint and next add executable/symbolic move generation or a dedicated coordinate-localization objective. Summary: `artifacts/v41/summary.json`. |
| V4.2 localization corpus and Colab audit (2026-08-04) | Built 12,000 records covering hard start-candidate choice, exact start coordinates, anchor masks, cross-check letter sets, start-to-placement conversion, word-to-placement, plan ranking, and free play. Reserved 30 previously untouched generated-test games: ten whole games per localization, word-placement, and free gate. | Two fresh Google Colab CPU audits recomputed 2,875 start-position label sets and verified zero invalid labels, train/gate overlap, official overlap, or openings. Final exact Qwen lengths were p50/p95/p99/max `911`/`1,096`/`1,131`/`1,159`; frozen cap `1,216`. Final audit SHA-256 `de6911...5de1` was superseded after a schema-only normalization and rerun; corrected audit is `artifacts/v42/colab/v42_colab_audit_final.json`. |
| V4.2 free Colab T4 probe | Allocated a free T4, transferred the 119 MB V4.1 Stage 2 adapter in four chunks, reassembled it to SHA-256 `d99649...8782`, and trained the new localization objective through checkpoint 100. | Loss fell from `5.2247` at step 10 to `3.1656` at step 100 (minimum logged window `2.8811` at step 80). Colab then reset/evicted the runtime and erased the remote checkpoint weights, leaving only the locally downloaded trainer state. Paid V4.2 spend remained `$0`; the probe justified a persistent fallback. |
| V4.2 gated Hugging Face launch | First Job `Cochon123/6a727b9b6b79c09949c22aa5` failed before execution because the V4.1 model volume could not mount. Moved the initialization adapter into the dataset mount via Hub deduplication and launched replacement Job `Cochon123/6a727c31a00abefd4b292c3d` on one L40S. | Replacement reached `RUNNING`, loaded the base and adapter, and began the frozen 400-record baseline. Hard cap is 1.5 hours/`$2.70`; Stage 1 requires ≥50% start-choice and ≥25% exact start localization, and Stage 2 requires ≥20% legal word-to-placement before free training may run. |
| V4.2 Stage 1 completion and stop (2026-08-05) | Replacement Job `Cochon123/6a727c31a00abefd4b292c3d` completed 500 localization steps in 1,328.6 training seconds. Overall train loss was `2.76034`; logged loss fell from `5.0560` at step 10 to mostly `1.85–2.41` late in training, with a final-window spike to `3.2557`. | On 100 paired fresh boards, candidate-start choice improved 8→87% but unconstrained start localization improved only 4→12%. The predeclared 50%/25% gate therefore failed and Stages 2–3 did not run. Total job cost was `$0.973`. |
| V4.2 matched localization diagnosis | Audited target/prediction index distributions and coordinate errors. Candidate targets covered all 16 shuffled indices; the 87% result was not an index shortcut. Paired candidate gains/losses were 81/2. Direct localization gains/losses were 10/2 (exact McNemar p≈`.039`). | Row correctness improved 19→31%, column correctness 15→22%, and mean Manhattan error 6.52→5.51 squares. The model can compare supplied locations but still cannot emit reliable coordinates without candidates. Baseline V4.1 Stage 2 happened to score 3/100 legal free moves and 1/100 legal word-hint moves on the untouched gates; V4.2 was not evaluated on those after the stop. Summary: `artifacts/v42/summary.json`. |
| V4.2 free-move pass@32 audit (2026-08-05) | Froze 20 benchmark-shaped ply-6/10/14 boards from 20 untouched generated-test games, excluding 70 prior diagnostic/gate games and every official/train overlap. Sampled 32 short free moves per board from V4.2 Stage 1 and V4.1 Stage 2. Five-board calibration selected temperature `.7`; temperature `.9` produced zero legal moves. | V4.2 found a legal move on 6/20 boards (30% pass@32), including one exact-optimal move; 6/640 individual samples were legal, and oracle best-of-32 score ratio was 13.75%. V4.1 found legal moves on 4/20 boards (20%), none optimal, with 5.94% oracle score. Only one success board overlapped, so the union covered 9/20 boards. Job `Cochon123/6a72aecf6b79c09949c22bf5` cost `$0.468`. Summary: `artifacts/v42/passk_summary.json`. |
| V4.2 pass@32 decision | The sampled policy is highly diverse (≈31.9 unique parsed moves per 32) but sparse: V4.2 reward groups vary on 30% of boards while 70% contain only invalid moves. Collisions (234/634 invalids) and invalid main words (188/634) dominate. | Do not spend next on row/column classification or immediate online GRPO. First harvest on-policy legal winners with both complementary checkpoints, pair them against their own collision/main-word failures, mix in solver-optimal anchors, and run a small offline verifier-filtered SFT/preference stage. Re-test greedy legality before any online RL. |
| V4.3 verifier-filtered corpus (2026-08-05) | Sampled 16 moves from both V4.2 and V4.1 Stage 2 on 160 train-only positions from 160 distinct games, with zero official, prior-V4, or frozen-evaluation overlap. V4.2 found legal candidates on 27/160 boards (28/2,560 samples); V4.1 found them on 6/160. No sampled move was optimal. Built 1,200 SFT records: 640 error-conditioned on-policy recoveries targeting the exact solver move, 160 solver-optimal anchors, and 400 rehearsal records. Reserved 640 preference pairs without training them. | Corpus manifest: `artifacts/v43/hf/corpus/manifest.json`. The absence of optimal on-policy positives limited this to supervised recovery rather than genuine reward optimization. |
| V4.3 training completion | Job `Cochon123/6a72c9cd6b79c09949c22cae` trained 200 QLoRA steps from V4.2 Stage 1 on one L40S. | Training took 575.3 seconds, mean loss `1.58681`, and remained numerically stable. The complete job used 5,891 billed seconds and cost approximately `$2.936`. Model: `Cochon123/Qwen3-4B-Scrabble-General-v4.3-onpolicy`. |
| V4.3 frozen evaluation | Reused the untouched 20-board pass-k set and 200-example localization gate. Greedy legality fell 1/20→0/20. Pass@16 rose 3/20→4/20 and individual legality rose 0.94%→1.56%, but optimal pass fell 5%→0% and oracle score fell 9.85%→8.83%. The pass@16 success sets had zero overlap (union 7/20). Start-choice fell 87%→83%; direct start-location fell 12%→7%. | Reject V4.3 as a promotion candidate and retain V4.2. V4.3 is only a complementary sampler: it expands legal-board coverage but displaces prior behavior and lowers move quality. Summary: `artifacts/v43/summary.json`. |

## V5 8B capacity pilot

| Phase | Event | Outcome |
|---|---|---|
| Clean 8B curriculum | Built 3,000 train and 120 validation records from 843 distinct train games, mixing start choice/location, anchor and cross-check tasks, constrained plans, ranking, and free moves. Frozen gates, official positions, train, and validation had zero overlap. Thinking was disabled and the exact Qwen3-8B token audit retained every record under a 1,856-token cap. | This was a controlled capacity test rather than a continuation of a 4B adapter. QLoRA rank 16/alpha 32 trained all-linear modules from `Qwen/Qwen3-8B`. |
| V5 8B training completion (2026-08-05) | Job `Cochon123/6a733570a00abefd4b293aa9` completed 400 steps on one A100 80 GB. Training took 2,224.6 seconds with mean loss `1.96115`; the full baseline, training, and gate run took 4,227 billed seconds. | Estimated cost was `$2.921`. The adapter and all raw evaluation artifacts were uploaded to private repository `Cochon123/Qwen3-8B-Scrabble-Capacity-Pilot`. |
| V5 8B hard promotion gate | Reused the untouched 20-board pass-k set plus held-out 200-example localization and 100-example constrained-plan gates. Observed/required: start choice 62/85%, start location 3/20%, pass@16 board legality 0/25%, oracle score 0/12%, plan-copy legality 84/90%, and plan-ranking legality 86/98%. | Failed all 6 predeclared gates; decision `stop_8b_pilot`. The model improved over its 8B base on candidate choice (10→62%), plan-copy legality (50→84%), and plan-ranking legality (20→86%), but produced 0 legal moves in 320 free samples. This rejects model size alone as the next lever. Summary: `artifacts/v5_8b/summary.json`. |

Machine-readable curves and evaluations are assembled by
`scripts/build_experiment_report.py`.

## V10 thinking-distillation free-compute pilot

| Phase | Event | Outcome |
|---|---|---|
| Teacher control (2026-08-07) | Asked Kimi K3 High for compact visible search traces. One unaided trace proposed `TAXI` but missed that it extended an existing word into invalid `PATENTAXI`; it was rejected. Re-ran eight train-only boards with the verified optimal target supplied only to the offline teacher. | All 8/8 oracle-assisted traces reproduced an optimal legal move and were verifier-accepted. No official position was used. |
| Frozen corpus and gate | Built 1,648 thinking-template records from 800 train positions, including 48 replicas of the eight Kimi traces, plus 80 validation records. Reserved a fixed 20-position generated-test gate with zero whole-game or official overlap. | Exact full-sequence maximum was 1,381 tokens under a 1,536-token cap. Deployment generation remained capped at 512 tokens. |
| Free Colab training | Trained `Qwen/Qwen3-4B-Thinking-2507` rank-16 QLoRA on free T4s through step 150, exporting complete optimizer checkpoints at 50, 100, and 150. A first VM reset erased a long evaluation but not the locally downloaded checkpoint; a second free T4 restored the checkpoint through a hash-checked seven-part upload. | Paid compute remained `$0`. Checkpoint archive hashes are recorded in `artifacts/v10_compact_reasoning/summary.json`; all Colab sessions were released at close. |
| Paired five-board gate | Reused the first five frozen gate boards at base, step 50, step 100, and step 150. Base/50/100/150 legality was 0/5 at every point and score was 0/123 at every point. Median completion length changed 512→272→206→224 tokens. | Distillation made inference substantially shorter but did not produce legal play. Failures collapsed onto invented words/anchor coordinates and occupied-square collisions. |
| Corrective ablation | From step 100, changed the compact verifier line to explicitly separate `new=letter@row,col` from `existing=...`, then trained 50 more free steps. Also tested dropping only exact same-letter duplicate placements after generation. | The model learned to emit the new field syntactically, but step 150 remained 0/5 legal; exact-duplicate removal recovered 0/5. The error is search/localization, not serialization. |
| Budget decision | Applied the predeclared rule that paid Hugging Face continuation requires positive held-out legality on free compute. | No paid Job was launched. The full `$5` budget was preserved because lower loss and shorter outputs did not justify extrapolating from zero legal moves. |

## V11 atomic-pointer legality pilot

| Phase | Event | Outcome |
|---|---|---|
| Distinct hypothesis (2026-08-08) | Replaced numeric coordinates with 225 existing Qwen vocabulary glyphs tied between every input square and the output start square. Used `WORD|GLYPH|A_OR_D`, deterministic verifier rendering, easiest-legal rather than solver-optimal SFT targets, and a board-complexity curriculum. | The remote tokenizer audit rejected the first glyph table because five tokens merged with delimiters. A rebuilt table passed with 225/225 unique atomic IDs in board and action contexts. Prompt median was 785 tokens; every measured completion was exactly 5 tokens. |
| Leakage-safe corpus | Generated 500 opening and 500 one-anchor training positions, then enumerated 300 controlled-cross and 700 full-board train positions. Frozen gates contain 100/100/40/144 positions. | Final audit found zero shared position IDs and zero shared game lineages between any optimizer data and any gate. All 77 tests and 11 subtests passed. |
| Free opening gate | Trained fresh Qwen3-4B-Instruct rank-16 QLoRA for 50 effective-batch-4 steps on a free Colab T4. | Untouched opening legality moved from 0/20 base to 81/100 greedy, with 100% parseable five-token actions. The predeclared 80% opening gate passed. |
| Colab fault recovery | The first anchor run reached step 50 but Colab erased its filesystem between archive creation and download. A replacement T4 was reclaimed before step 25. Restored the locally hash-verified opening adapter to a third T4 and changed only checkpoint granularity/gradient accumulation so every 25 examples could be exported. | Recovered step 25/50/75/100 anchor checkpoints locally; final archive SHA-256 `cfe49e...618aa`. No paid compute was used. |
| One-anchor learning curve | Trained 100 single-example optimizer steps with opening replay. Evaluated frozen greedy legality after each 25-step segment. | One-anchor legality rose 1%→11%→21%→27%; opening legality was 91% at step 25, 85% at step 50, and 93% at step 100. The tied pointer representation produced real localization transfer, but missed the required 40% one-anchor gate by 13 points. |
| Genuine rejection-only retries | On the 100 frozen one-anchor boards, sampled up to three attempts. Each retry saw only its own prior rejected action and a generic rejection; no solver move or error-specific solution was inserted. | Legal coverage rose 27→34 boards and yielded seven genuine on-policy legal retry records. This is materially different from V4.3/V7 correction records that replaced failures with solver-optimal targets. |
| Initial budget decision | Applied the predeclared promotion requirements: at least 80% greedy opening legality and 40% greedy one-anchor legality before cross/full training or paid verifier optimization. | Opening passed at 93%; one-anchor initially failed at 27%. Stopped that run, released every Colab session, launched no Hugging Face Job, and preserved the full `$5` budget. |
| User-authorized continuation (2026-08-08) | Resumed only the anchor curriculum from the hash-verified step-100 archive and compared short, separately seeded scheduler/batch continuations. A 25-step `5e-5` branch damaged parseability (78%) and reached 21% legality; a batch-1 `1e-5` branch reached 37% at step 25 and regressed to 34% at step 50. | A 25-step `1e-5`, effective-batch-4 branch reached 44/100 one-anchor legality while retaining 93/100 opening legality, both 100% parseable. This passed the original 40%/80% promotion gate and identifies optimization noise—not representation alone—as part of the earlier miss. |
| Controlled-cross transfer | From the promoted anchor checkpoint, trained controlled-cross examples with opening/anchor replay. Frozen gate legality was 2/40 at step 25 and 3/40 at step 50. A lower-rate 25-step continuation was repeated after one Colab reset erased an undownloaded branch. | The final recoverable retry remained 3/40 (7.5%), 100% parseable, below the predeclared 4/40 (10%) gate. Dominant errors were occupied-square letter mismatches, rack-supply errors, and invalid cross words. |
| Final budget decision | Required controlled-cross legality of at least 4/40, plus 80% opening and 35% anchor retention, before full-board training or paid verifier optimization. | Cross transfer missed by one board, so retention was not re-tested, full-board training was not launched, and no Hugging Face Job was started. Paid spend remained `$0`; the entire `$5` budget is preserved. All Colab sessions were released. Summary: `artifacts/v11_atomic_policy/summary.json`. |
| Adversarial identifiability audit (2026-08-09) | A separate review found the stage-2 corpus collapsed to one-new-tile hooks and overwhelmingly two-letter targets. Generated a new 200-board, lineage-disjoint controlled-cross gate (SHA-256 `867bd2...9f4d7`) and greedily evaluated the exact same boards against the pre-cross and all three cross checkpoints. | Pre-cross `stage1d` was 19/200 (9.5%); stage2a step 25 was 13/200 (6.5%), stage2a step 50 was 17/200 (8.5%), and stage2c step 25 was 21/200 (10.5%). The best cross model improved only +2 pp, below the predeclared causal-gain requirement of +5 pp. Independent 16-sample coverage was attempted free on a T4 but hit its 30-minute cap without an artifact; it is recorded as incomplete, not zero. No RL or paid Job is justified. |
| True-cross legal-set control (2026-08-09) | Built 500 new train prompts, 80 validation prompts, and a frozen 400-board gate. Each train prompt has eight verifier-certified actions that add at least one tile and create at least one perpendicular word; targets were stratified across direction, length, region, new-tile count, and cross count. Exact K=8 marginal-likelihood training OOMed on the free T4 before step 1; K=2 was stopped as too slow. Ran the preregistered 125-step randomized single-positive SFT control from stage1d instead. | On the identical 400-board gate, stage1d was 43/400 (10.75%) and the control was 26/400 (6.50%). Paired outcomes were 38 baseline-only vs 21 trained-only; exact one-sided McNemar p=`0.9908`, net `-4.25` points. Existing-tile mismatches fell 289→250 (13.6% relative, below the required 30%), while old retention gates fell from 93→55% opening and 44→16% one-anchor. The control failed every promotion criterion; no verifier RL, full-board continuation, or paid Hugging Face Job was launched. Artifacts and hashes are in `artifacts/v11_atomic_policy/summary.json`. |
| Exact K=8 MML Hugging Face run (2026-08-09) | User authorized a paid test after the free T4 could not fit the exact objective. A first A100 run was canceled after a few minutes when review found an erroneous `/K` loss scaling; the corrected run `Cochon123/6a78f8a5da2af92a634f09be` completed all 125 steps on an A100 80 GB. It used 12.6 GB peak allocated GPU memory and 3,504 training seconds; total corrected-job wall time was 3,729 seconds (about `$2.59`), with total V11 paid spend approximately `$2.8` including the short canceled run. | The exact multi-positive objective catastrophically collapsed the output grammar: `0/400` parseable and `0/400` legal greedy moves on the same frozen true-cross gate. Typical outputs were repeated high-prior fragments such as `AEAE...` and `||||...`, rather than `WORD|SQUARE|A_OR_D`. This rejects the true-cross MML objective as currently formulated; no further verifier RL or paid continuation is warranted. Status/gate hashes are recorded in `artifacts/v11_atomic_policy/summary.json`. |

## V12 score-blind full-legal-space ranking

| Phase | Event | Outcome |
|---|---|---|
| Architecture contract (2026-08-13) | Separated the hybrid product from the learned-ranking question. A symbolic enumerator guarantees legality, while all solver scores and optimality labels live in evaluator-only files. Model inputs contain only visible board/rack state plus word, geometry, placements, formed crosses, and premium-square features. | Recursive score-blind audits passed. The immutable 300-board V8 artifact hash was reused, and train/validation/test source-game overlap was zero. Exact symbolic score remains the correct production choice if it is allowed at inference; V12 tests whether score structure can be learned without exposing that scalar. |
| CPU pilot | Enumerated complete legal spaces for 240 lineage-separated training boards (103,042 candidates) and all 144 validation positions from 16 held-out games (70,197 candidates). Trained a dependency-free standardized pairwise linear ranker with 15 score-blind features and 14,883 training pairs. | Validation reached 84.03% exact-optimal top-1, 97.24% point ratio, and 99.31% recall@32. The paired 95% lower bound over face-value was +62.48 points. All promotion gates passed without accelerator compute. |
| Frozen 300-board gate | Evaluated the frozen model once on 117,465 legal plans from 300 unseen games. | Reached 83.00% exact-optimal top-1, 97.60% point ratio, and 100% recall@32. Top-1 remained 80.39% on the 51 boards with more than 512 candidates. The paired lower bound over face-value was +60.73 points. |
| Official confirmation | Before looking at the official result, froze model SHA-256 `78293c...b450`, official dataset SHA-256 `8a2fd0...9e32`, and a 50% exact-top-1 confirmation gate. Evaluated all 100 official positions without retraining or feature changes. | Confirmed 74/100 exact-optimal, 93.66% point ratio, and 100% recall@32. The >512-candidate slice was 60%; face-value was 15.2% top-1. This is a hybrid ranking diagnostic and does not test or clear the unaided-generation goal. Summary: `artifacts/v12_legal_ranking/summary.json`. |
| Correction and decision | User clarified that solver-enumerated candidate ranking was not the intended test. | V12 changed the target instead of testing unaided board+rack → move generation. Retain its code and artifacts only as an off-target diagnostic, never report 74% as progress toward the project goal, and obtain explicit agreement before running a replacement experiment. |
